server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: modulo-admin          
          uri: lb://servicio-administrador  
          predicates:
            - Path=/api/administradores/**

        - id: modulo-proveedores
          uri: lb://PROVEEDORES
          predicates:
            - Path=/api/proveedores/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/